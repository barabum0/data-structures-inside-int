# Демонстрации пятого дня

Пятнадцать программ: одиннадцать про хеш-таблицу и четыре про сборку
программы из нескольких файлов. Запустите каждую сами.

**Здесь только команды и вопросы.** Ни вывода программ, ни объяснений: всё,
что нужно для ответа, было на лекции или добывается запуском. На каждый вопрос
отвечайте **до** запуска, письменно или вслух, а потом проверяйте себя. Место,
где предсказание разошлось с выводом, и есть то, что вы сегодня узнали.

Всё снималось на учебной машине: `gcc 15.2`, Linux x86-64. Время работы у вас
будет своим, отношения времён — теми же.

Демонстрации 11–14 лежат в отдельных каталогах: в каждой из них по несколько
файлов, и это их содержание. Собираются они по командам из этого файла,
`make` там нет.

|                                             | о чём                                                   |
| ------------------------------------------- | ----------------------------------------------------------- |
| [01-hash-is-a-number](#01-hash-is-a-numberc) | что такое хеш                                     |
| [02-hash-sum](#02-hash-sumc)                 | сумма байтов в роли хеша                   |
| [03-distribution](#03-distributionc)         | раскладка по корзинам                       |
| [04-birthday](#04-birthdayc)                 | когда случится первая коллизия     |
| [05-chaining](#05-chainingc)                 | цепочки                                             |
| [06-probing](#06-probingc)                   | открытая адресация                           |
| [07-tombstone](#07-tombstonec)               | удаление и надгробия                       |
| [08-load-factor](#08-load-factorc)           | цена заполненности                           |
| [09-rehash](#09-rehashc)                     | таблица, которая растёт                   |
| [10-adversary](#10-adversaryc)               | все ключи в одной корзине                 |
| [11-two-files](#11-two-files)                | две единицы трансляции                     |
| [12-header](#12-header)                      | зачем нужен заголовок                     |
| [13-static](#13-static)                      | что делает`static`                                |
| [14-guard](#14-guard)                        | страж включения                               |
| [15-compact-dict](#15-compact-dictc)         | из чего сложен`dict`                            |

---

## 01-hash-is-a-number.c

```sh
gcc -std=c11 -Wall -Wextra 01-hash-is-a-number.c -o 01-hash-is-a-number
./01-hash-is-a-number
```

В списке восемь строк. Какие две из них обязаны получить одно и то же число?
Ответьте до запуска.

Пустая строка тоже получила хеш. Посмотрите на функцию и скажите, откуда он
взялся, если в цикле не было ни одного шага.

Строки `a` и `b` различаются одним битом в одном байте. На сколько бит
разошлись их хеши? Сформулируйте, зачем хеш-функции такое свойство.

Теперь питон:

```python
print(hash("hello"))
```

Запустите эту строку дважды в одном интерпретаторе, а потом ещё раз в новом.
Сравните с тем, что делает ваша программа при двух запусках подряд, и скажите,
какое свойство хеша при этом всё равно сохраняется.

---

## 02-hash-sum.c

```sh
gcc -std=c11 -Wall -Wextra 02-hash-sum.c -o 02-hash-sum && ./02-hash-sum
```

Шесть пар слов. До запуска отметьте те, у которых числа совпадут, и
сформулируйте признак одним предложением.

`sum_hash` — законная хеш-функция: равные строки она переводит в равные числа.
Скажите, какое ещё свойство от хеш-функции требуется и какое из двух здесь
нарушено.

Последняя строка вывода считает, сколько разных значений выдала сумма на
двухстах ключах. Объясните полученное число, посчитав границы суммы для ключей
вида `keyNNN`.

В Python проверьте то же самое:

```python
print(hash("listen") == hash("silent"))
```

Скажите, что этот ответ говорит про питоновскую хеш-функцию.

---

## 03-distribution.c

```sh
gcc -std=c11 -Wall -Wextra 03-distribution.c -o 03-distribution
./03-distribution
```

Двести ключей, шестьдесят четыре корзины. Сколько ключей попало бы в корзину
при равномерной раскладке? Назовите число до запуска.

Посмотрите на первую гистограмму. Сколько корзин осталось пустыми и почему
занятые идут подряд? Свяжите с последней строкой предыдущей демонстрации.

Во второй гистограмме пустых корзин нет. Сравните самую длинную корзину в двух
раскладках и скажите, во сколько раз подорожает поиск в худшем случае.

Индекс корзины берётся как `hash % 64`. Какая часть хеша при этом используется?
Ответьте в терминах двоичной записи числа.

---

## 04-birthday.c

```sh
gcc -std=c11 -Wall -Wextra 04-birthday.c -o 04-birthday -lm
./04-birthday
```

Ключи случайные, хеш хороший, корзин 1024. Сколько ключей удастся положить,
прежде чем два попадут в одну корзину? Назовите число до запуска.

Посмотрите на два столбца — «первая коллизия» и `sqrt(cap)`. Какое между ними
отношение и сохраняется ли оно по всей таблице?

Последний столбец говорит, какая доля таблицы была занята к этому моменту.
Сформулируйте, что из этого следует для любой хеш-таблицы.

Вторая часть вывода — про то, когда коллизия перестаёт быть вопросом
вероятности. Объясните это одним предложением, не используя слова «случайно».

Поменяйте в программе случайные ключи на последовательные — `key0`, `key1`,
`key2` — и запустите снова. Числа изменятся сильно; объясните направление
изменения, глядя на то, какие байты ключа влияют на младшие биты хеша.

---

## 05-chaining.c

```sh
gcc -std=c11 -Wall -Wextra 05-chaining.c -o 05-chaining && ./05-chaining
```

Тысяча ключей, число корзин уменьшается вдвое на каждой строке. Во сколько раз
вырастет столбец «сравнений/поиск» при переходе от 2048 корзин к 1024?
Ответьте до запуска.

Посмотрите на столбцы «загрузка» и «сравнений/поиск» вместе. Выведите формулу,
связывающую их, и проверьте её на трёх строках таблицы.

На строке с 4096 корзинами загрузка 0.24, а сравнений всё равно больше одного.
Откуда берётся эта добавка?

Последняя строка — 8 корзин на тысячу ключей. Какой структурой данных стала
таблица и какова сложность поиска в ней?

Столбец «пустых» на верхних строках больше половины. Объясните, почему при
загрузке 0.24 пустует не 76% корзин, а больше.

---

## 06-probing.c

```sh
gcc -std=c11 -Wall -Wextra 06-probing.c -o 06-probing && ./06-probing
```

Ключей двенадцать, слотов шестнадцать, цепочек нет — ключ лежит прямо в
таблице. Сколько слотов придётся просмотреть, чтобы положить первый ключ?

Следите за картой после каждой вставки. Найдите первый ключ, которому не
досталась его собственная корзина, и скажите, куда он лёг.

Последний ключ просмотрел заметно больше слотов, чем первые. Посмотрите на
карту перед его вставкой и объясните число.

Занятые слоты собираются в сплошные участки. Объясните, почему длинный участок
растёт быстрее короткого.

Ключ `brown` лёг в слот с меньшим номером, чем его корзина. Что произошло и
какая строка в коде за это отвечает?

---

## 07-tombstone.c

```sh
gcc -std=c11 -Wall -Wextra 07-tombstone.c -o 07-tombstone && ./07-tombstone
```

Три ключа одной корзины лежат в трёх соседних слотах. Поиск идёт от корзины
вперёд и останавливается на первом пустом слоте. Что вернёт поиск третьего
ключа, если освободить слот второго? Ответьте до запуска.

Сравните два блока вывода. Скажите, что именно сломалось в первом и какой
одной строкой это чинится во втором.

Надгробие — не пустой слот и не ключ. Выпишите, как ведут себя при встрече с
ним поиск и вставка, и объясните, почему эти два ответа разные.

В последнем блоке новый ключ занял место надгробия. Что было бы, если бы
вставка проходила надгробия насквозь, как поиск?

Надгробия накапливаются. Посмотрите на демонстрацию 08 и скажите, чем это
опасно для таблицы, в которой ключи часто удаляют и добавляют.

---

## 08-load-factor.c

```sh
gcc -std=c11 -Wall -Wextra 08-load-factor.c -o 08-load-factor
./08-load-factor
```

Во сколько раз подорожает поиск, когда занято 99% слотов вместо 50%? Назовите
число до запуска, а потом посмотрите на таблицу.

Два последних столбца различаются в разы. Объясните, почему поиск
отсутствующего ключа дороже поиска существующего.

Найдите в таблице строку, после которой числа начинают расти резко. Сравните
её с тем порогом, который используют реальные хеш-таблицы.

Все строки таблицы получены на одном и том же числе слотов. Скажите, что
именно меняется от строки к строке и почему само по себе число ключей здесь
ничего не решает.

Свяжите с предыдущей демонстрацией: надгробия занимают слоты, но не хранят
ключей. К какому столбцу этой таблицы относится таблица с надгробиями?

---

## 09-rehash.c

```sh
gcc -std=c11 -Wall -Wextra 09-rehash.c -o 09-rehash && ./09-rehash
```

Сто тысяч ключей, таблица начинает с восьми слотов и удваивается при
заполнении на три четверти. Сколько раз она переедет? Ответьте до запуска.

Сколько всего ключей будет переложено за все переезды? Назовите число до
запуска и сравните с вашей вчерашней оценкой для вектора.

Поделите «переложено» на число ключей. Полученное число не зависит от того,
сто тысяч ключей или миллион, — объясните почему.

В конце вывода напечатана итоговая заполненность таблицы. Почему она заметно
меньше трёх четвертей?

Переезд стоит дороже, чем перенос такого же числа элементов в векторе.
Скажите, что именно приходится делать с каждым ключом и чего в векторе не было.

Замените порог 3/4 на 1/2 в одном месте и запустите снова. Что изменится в
числе переездов, а что — в результате демонстрации 08?

---

## 10-adversary.c

```sh
gcc -std=c11 -Wall -Wextra 10-adversary.c -o 10-adversary && ./10-adversary
```

Пять тысяч ключей — перестановки одних и тех же десяти букв. Что сделает с
ними сумма байтов? Ответьте до запуска, вспомнив демонстрацию 02.

Посмотрите на столбцы «busy» и «longest». Скажите, во что превратилась таблица
в первой строке.

Число сравнений в первой строке — это сумма арифметической прогрессии.
Получите его формулой и сверьте с выводом.

Отношение времён и отношение числа сравнений — разные числа. Объясните, почему
второе не равно первому.

Ключи здесь подобраны специально. Представьте, что ключи приходят из сети и их
выбирает тот, кто знает вашу хеш-функцию. Сформулируйте, что он получит, и
скажите, что этому противопоставляет Python (посмотрите `PYTHONHASHSEED` в
документации).

---

## 11-two-files

```sh
cd 11-two-files
gcc -std=c11 -Wall -Wextra -c main.c -o main.o
gcc -std=c11 -Wall -Wextra -c area.c -o area.o
gcc main.o area.o -o prog
./prog
```

Компилятор обрабатывал `main.c`, ничего не зная про `area.c`. Откуда он взял
адрес функции `rect_area`?

```sh
nm main.o
nm area.o
```

`nm` печатает имена, которые есть в объектном файле. Найдите `rect_area` в
обоих и объясните разницу между буквами `U` и `T` рядом с именем.

```sh
gcc -std=c11 -Wall -Wextra main.c -o prog
```

Скажите, какая программа напечатала сообщение об ошибке — компилятор или кто-то
после него. Найдите в сообщении имя, которое осталось необъяснённым.

Что изменится, если удалить `#include "area.h"` из `main.c`, а собирать
по-прежнему из двух файлов? Проверьте.

---

## 12-header

```sh
cd 12-header
gcc -std=c11 -Wall -Wextra main.c counter.c -o prog
./prog
```

В `counter.h` одна строка, в `counter.c` — две функции, одна из которых
`static`. Скажите, что из этого попало в заголовок и по какому признаку.

```sh
gcc -std=c11 -Wall -Wextra no-header.c counter.c -o prog2
```

Тот же вызов, но без `#include`. Прочитайте оба сообщения компилятора: первое
про необъявленную функцию, второе про формат. Объясните, откуда взялось второе,
если про функцию компилятор ничего не знает.

Переменная `total` объявлена `static`. Что произойдёт, если это слово убрать, а
рядом с `counter.c` появится другой файл с переменной с тем же именем?

Заголовок включается и в `main.c`, и в `counter.c`. Зачем он нужен во втором,
если там же лежат сами определения?

---

## 13-static

```sh
cd 13-static
gcc -std=c11 -Wall -Wextra -c main.c -o main.o
gcc -std=c11 -Wall -Wextra -c util.c -o util.o
gcc main.o util.o -o prog
```

Обе функции определены в `util.c`, обе объявлены в `main.c`. Одна из двух строк
`main` до программы не доберётся — какая? Ответьте до сборки.

На каком шаге это выяснилось: при компиляции `main.c`, при компиляции `util.c`
или позже? Объясните, почему именно там.

```sh
nm util.o
```

Найдите обе функции. Буквы рядом с именами разные — скажите, какая из них
означает «видно только внутри файла».

Уберите `static` и соберите снова. Потом объясните, зачем это слово вообще
нужно, если без него всё работает.

---

## 14-guard

```sh
cd 14-guard
gcc -std=c11 -Wall -Wextra main.c point.c -o prog
```

`main.c` включает `point.h` напрямую, а `shape.h` включает его же ещё раз.
Препроцессор подставляет текст заголовка в место `#include`. Что окажется в
тексте `main.c` после подстановки?

Прочитайте сообщение об ошибке. Про что оно — про структуру или про функцию?
Объясните, почему повторное объявление функции ошибкой не стало.

Посмотрите на результат работы препроцессора целиком:

```sh
gcc -std=c11 -E main.c | grep -n "struct Point" 
```

Добавьте в `point.h` страж включения:

```c
#ifndef POINT_H
#define POINT_H
...
#endif
```

Соберите снова и объясните, что именно мешает второй подстановке.

Какое из двух включений в `main.c` теперь лишнее? Проверьте, убрав его, и
скажите, почему полагаться на это всё-таки не стоит.

---

## 15-compact-dict.c

Это вопрос дня.

```sh
gcc -std=c11 -Wall -Wextra 15-compact-dict.c -o 15-compact-dict
./15-compact-dict
```

Восемь ключей, шестнадцать корзин, два способа хранения. До запуска откройте
питон и посмотрите, в каком порядке печатается словарь из тех же ключей:

```python
d = {"apple": 1, "banana": 2, "cherry": 3, "date": 4,
     "elder": 5, "fig": 6, "grape": 7, "kiwi": 8}
print(list(d))
```

Сравните два «обхода» в выводе программы. Какой из них совпал с питоновским и
почему второй совпасть не мог?

Посмотрите на строку «корзины» во втором способе. Что лежит в корзине вместо
записи и сколько это занимает байт?

После удаления `cherry` в корзинах появилось `-2`, а в записях — дыра. Скажите,
что произойдёт с этой дырой при следующем росте таблицы и почему порядок
остальных ключей при этом не пострадает.

Последняя таблица считает память для обоих способов. Объясните, почему для
миллиона ключей разница именно такая, и скажите, какой из двух столбцов растёт
скачками, а какой — плавно.

Проверьте порядок в питоне ещё раз:

```python
d = {"a": 1, "b": 2, "c": 3}
del d["b"]
d["b"] = 4
print(list(d))
```

Объясните полученный порядок через устройство, которое вы только что видели.
